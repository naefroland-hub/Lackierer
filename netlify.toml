# netlify.toml – Konfiguration für das Lerne-Lack Netlify-Deployment

[build]
  publish = "."

[functions]
  directory = "netlify/functions"

# /api/gemini → Netlify Function (versteckt den API-Key)
[[redirects]]
  from   = "/api/gemini"
  to     = "/.netlify/functions/gemini"
  status = 200
